import { PasswordRules } from './password-rules.directive';

describe('PasswordRules', () => {
  it('should create an instance', () => {
    const directive = new PasswordRules();
    expect(directive).toBeTruthy();
  });
});
